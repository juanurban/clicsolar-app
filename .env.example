# Configuración de Base de Datos MySQL (Hostinger)
NODE_ENV=production
DB_HOST=localhost
DB_PORT=3306
DB_USER=u123456789_sunquote
DB_PASSWORD=TuContraseñaMySQL
DB_NAME=u123456789_sunquote
PORT=3000
GROQ_API_KEY=tu_clave_groq
GROQ_MODEL=openai/gpt-oss-120b
# PDF: Generación exacta de la vista previa.
# Puppeteer incluye Chromium embebido que se descarga automáticamente.
# Si falla en Hostinger, instala las dependencias del sistema:
#   sudo apt-get install -y libnss3 libxss1 libasound2 libatk-bridge2.0-0 libgtk-3-0 libgbm1
# O instala Chromium directamente:
#   sudo apt-get install -y chromium-browser
# Y luego configura la ruta aquí:
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser
